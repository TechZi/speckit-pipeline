interface:
  display_name: "Spec Kit Pipeline"
  short_description: "Run gated Spec Kit features through convergence"
  default_prompt: "Use $speckit-pipeline to run or resume one feature through planning, implementation, verification, and convergence."

policy:
  allow_implicit_invocation: false
